{
  "name": "graphql",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^2.8.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bootstrap": "yarn install && lerna bootstrap",
    "start": "yarn install && lerna bootstrap && lerna run --parallel start"
  },
  "author": "",
  "license": "ISC"
}
