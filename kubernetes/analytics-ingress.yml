apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: analytics-ingress
  namespace: test-reactive-analytics
  annotations:
    kubernetes.io/ingress.class: 'nginx'
spec:
  tls:
    - hosts:
        - test-reactive-analytics.adaptivecluster.com
      secretName: nginx-ingress-tls
  backend:
    serviceName: analytics-client
    servicePort: 3000
  rules:
    - host: test-reactive-analytics.adaptivecluster.com
      http:
        paths:
          - path: /
            backend:
              serviceName: analytics-client
              servicePort: 3000

          - path: /subscriptions
            backend:
              serviceName: analytics-server
              servicePort: 4000

          - path: /graphql
            backend:
              serviceName: analytics-server
              servicePort: 4000

          - path: /healthz
            backend:
              serviceName: analytics-server
              servicePort: 4000
