apiVersion: v1
kind: Service
metadata:
  labels:
    app: reactive-analytics-client
  name: client-service
spec:
  selector:
    app: reactive-analytics-client
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: LoadBalancer
